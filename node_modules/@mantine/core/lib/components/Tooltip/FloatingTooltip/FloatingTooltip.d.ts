import React from 'react';
import { DefaultProps, MantineColor, ClassNames, MantineNumberSize } from '@mantine/styles';
import { SharedPopperProps } from '../../Popper';
import useStyles from './FloatingTooltip.styles';
export declare type FloatingTooltipStylesNames = ClassNames<typeof useStyles>;
export interface FloatingTooltipProps extends DefaultProps<FloatingTooltipStylesNames>, SharedPopperProps, React.ComponentPropsWithoutRef<'div'> {
    /** Tooltip content */
    label: React.ReactNode;
    /** Any react node that should trigger tooltip */
    children: React.ReactNode;
    /** Open delay in ms, 0 to disable delay */
    openDelay?: number;
    /** Close delay in ms, 0 to disable delay */
    closeDelay?: number;
    /** Any color from theme.colors, defaults to gray in light color scheme and dark in dark colors scheme */
    color?: MantineColor;
    /** Radius from theme.radius, or number to set border-radius in px */
    radius?: MantineNumberSize;
    /** True to disable tooltip */
    disabled?: boolean;
    /** Tooltip width in px or auto */
    width?: number | 'auto';
    /** Allow multiline tooltip content */
    wrapLines?: boolean;
    /** Get tooltip ref */
    tooltipRef?: React.ForwardedRef<HTMLDivElement>;
    /** Tooltip id to bind aria-describedby */
    tooltipId?: string;
    /** useEffect dependencies to force update tooltip position */
    positionDependencies?: any[];
    /** Whether to render the target element in a Portal */
    withinPortal?: boolean;
}
export declare const FloatingTooltip: React.ForwardRefExoticComponent<FloatingTooltipProps & React.RefAttributes<HTMLDivElement>>;
//# sourceMappingURL=FloatingTooltip.d.ts.map